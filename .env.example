# Web platform
BASE_URL=http://127.0.0.1:8000
SECRET_KEY=replace-with-long-random-string
# python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
TOKEN_FERNET_KEY=
DATABASE_URL=sqlite:///./data/usyd_due.db
# Postgres (Render): use the dashboard connection string; postgres:// is auto-normalized.
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/usyd_due

TIMEZONE=Australia/Sydney
DEMO_EMAIL=demo@usyd-due.local
GITHUB_URL=https://github.com/OceanHu123/usyd-due-reminders

# Mail — empty = print to console (local). On Render set REQUIRE_MAIL=true + Resend.
REQUIRE_MAIL=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=Usyd Due <noreply@example.com>
RESEND_API_KEY=
