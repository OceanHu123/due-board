# Web platform
BASE_URL=http://127.0.0.1:8000
SECRET_KEY=replace-with-long-random-string
# python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
TOKEN_FERNET_KEY=
DATABASE_URL=sqlite:///./data/due_board.db
# Postgres (Render): use the dashboard connection string; postgres:// is auto-normalized.
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/due_board

TIMEZONE=Australia/Sydney
DEMO_EMAIL=demo@due-board.local
GITHUB_URL=https://github.com/OceanHu123/due-board

# Mail — empty = print to console (local). On Render set REQUIRE_MAIL=true + Resend.
REQUIRE_MAIL=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=DueBoard <noreply@example.com>
RESEND_API_KEY=
