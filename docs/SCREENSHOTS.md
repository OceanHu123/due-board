# Screenshots checklist

Add images under `docs/images/` and embed them in the README when ready.

Suggested captures (desktop + one phone width):

1. **Landing** — hero line, Try demo + Sign in (`/`)
2. **Demo board** — several due cards, “Demo mode” banner (`/` after Try demo)
3. **Settings** — connected/not connected state, reminder hours (`/settings`) — use a real account, never show raw tokens
4. **Magic-link email** — subject + “Open board” link (blur address if needed)
5. **Privacy** — `/privacy`

Tip: use fictional course titles in screenshots if you prefer not to show live unit names.
